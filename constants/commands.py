from enum import Enum

class EmulatorCommand(Enum):
    UP = "up"
    DOWN = "down"
    LEFT = "left"
    RIGHT = "right"
    SELECT = "select"
    START = "start"
    A = "a"
    B = "b"
    L = "l"
    R = "r"
